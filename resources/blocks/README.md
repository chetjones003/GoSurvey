# Bundled CAD block library (WBLOCK-style drawings loaded as definitions on New / startup).

- `*.dxf` / `*.dwg` in this folder load as block definitions on startup.
- `fittings/*.sat` — 3D pipe fittings (ACIS solids as block definitions; see `fittings/README.md`).
